<?php

$tags = get_tags(array('sort' => 'alpha'), 200);
$tagnames = array_map(create_function('$e', 'return $e["name"] ;'), $tags);
$options = array_combine($tagnames,$tagnames);

?>

<div id="tagtext">
<?php echo $this->formSelect( $inputNameStem . "[text]", $value, array('class'=>'select'), $options); ?>
</div>

